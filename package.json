{
  "name": "dailee",
  "version": "1.0.0",
  "description": "Habits and tasks tracking",
  "author": "Stiven Castillo",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "server:dev": "yarn workspace server start:dev",
    "client:dev": "yarn workspace client dev"
  },
  "prisma": {
    "schema": "./server/prisma/schema.prisma"
  }
}
